// @ts-check

/**
 * Sidebar for the Contributor Guide docs instance (docs/contributor/, routed at
 * /contributing/). Ordering comes from each doc's `sidebar_position` front matter.
 *
 * @type {import('@docusaurus/plugin-content-docs').SidebarsConfig}
 */
const sidebars = {
  contributorSidebar: [{type: 'autogenerated', dirName: '.'}],
};

export default sidebars;
