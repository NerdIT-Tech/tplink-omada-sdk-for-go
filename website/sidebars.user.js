// @ts-check

/**
 * Sidebar for the User Guide docs instance (docs/user/, routed at /docs/).
 * Ordering comes from each doc's `sidebar_position` front matter.
 *
 * @type {import('@docusaurus/plugin-content-docs').SidebarsConfig}
 */
const sidebars = {
  userSidebar: [{type: 'autogenerated', dirName: '.'}],
};

export default sidebars;
